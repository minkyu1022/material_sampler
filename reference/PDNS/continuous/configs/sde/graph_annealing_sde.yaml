_target_: src.components.sdes.GraphAnnealedSDE
beta0: ${annealing_beta0}
beta1: ${annealing_beta1}
sigma: ${sigma}
max_grad_E_norm: ${max_grad_E_norm}
type: ${annealing_type}
n_particles: ${n_particles}
spatial_dim: ${spatial_dim}

